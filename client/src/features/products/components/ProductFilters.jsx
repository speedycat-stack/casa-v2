import React from "react";
import "./ProductFilters.css";

const ProductFilters = ({ filters, onFilterChange }) => {
  return (
    <div className="product-filters">{/* Filters removed as requested */}</div>
  );
};

export default ProductFilters;
